#include<iostream>
using namespace std;

int main(){
    int a =10;
    int b =20;

    if(a>b){        //in this case no output will print as a is less than b but we give a is greater than b
        cout<<"A is less than B";
    }
    /* if(a<b){        //in this case  output will print as a is less than b and we give a is less than b
        cout<<"A is less than B";
    }*/
    return 0;
}
